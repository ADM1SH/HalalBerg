# HalalBurg Terminal — Backend Dependencies
# Install with: pip install -r requirements.txt

# Core Django
django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
python-dotenv==1.0.1

# Market Data
yfinance==0.2.40

# HTTP client (for FMP, NewsAPI, GoldAPI, LM Studio)
requests==2.32.3
httpx==0.27.0

# Background job scheduler (price refresh every 60s)
apscheduler==3.10.4

# Utilities
python-dateutil==2.9.0
